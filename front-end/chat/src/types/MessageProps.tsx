import * as React from "react";

type MessagePropsType = {
  create_user_id: number;
  thread_id: number;
  collection_id: number;
  relate_num: number;
  search_method: string;
  model_name: string;
  message_text: string;
};

export default MessagePropsType;
